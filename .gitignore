__pycache__/
*.pyc
.pytest_cache/
bin/

# Только журнал в корне. Без ведущего слэша паттерн матчит любой уровень —
# и `audit/` съедал пакет internal/audit/, из-за чего он не попадал в git,
# а локально всё работало, потому что файлы лежат на диске.
/audit/

Icon?
